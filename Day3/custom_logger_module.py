# Custom Logger Module 
# Real-world app: Any production app — logging system 
# Problem 
# Build you own custom logger module (logger.py) that other Python files can import. The logger must write timestamped log messages to both the terminal and a log file. Support four log levels: INFO, WARNING, ERROR, DEBUG. 
# Each message must include timestamp, level, and message text. 